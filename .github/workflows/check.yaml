name: "Nix Flake Check"
on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths-ignore:
      - .github/**
concurrency:
  group: ooknet-check-${{ github.ref }}
  cancel-in-progress: true
jobs:
  nix-flake-check:
    name: Check Flake
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.KUNZEN_TOKEN }}
          submodules: true
      - name: Configure Git
        run: |
          git config --global url."https://${{ secrets.KUNZEN_TOKEN }}@github.com/".insteadOf git@github.com:
          git config --global url."https://${{ secrets.KUNZEN_TOKEN }}@github.com/".insteadOf ssh://git@github.com/
      - name: Setup Nix
        uses: DeterminateSystems/nix-installer-action@main
      - name: Magic Nix Cache
        uses: DeterminateSystems/magic-nix-cache-action@main
      - name: Nix Flake
        run: nix flake check
